The `$d` loop var got mangled by cmd→ssh→zsh again (the for-loop `$d` didn't expand). But I can see the .tgz files exist. The bare directories `L-0903-2113` etc. apparently don't exist as dirs (only .tgz) — the earlier `ls /tmp` showed both names AND .tgz, but the dirs might have been removed or the ls showed dir + its tgz. Actually the `ls /tmp` output listed both `2026-09-03.21-04-51` and `2026-09-03.21-04-51.tgz` — the non-.tgz entries are directories. But my for-loop failed to see them due to quoting. Let me inspect the .tgz contents directly without a loop (avoid the $d trap) — these tgz were created Sep 4 08:57/09:06 (very recent — during MY session? No, I didn't create them; timestamps 08:57-09:06 are around when I was copying logs. Hmm, 08:57 is when I ran docker cp. Did MY docker cp create these? No. Someone else — maybe the coordinating supervisor agent — is also active and packaged these bundles at 08:57/09:06. Or these are being made by another process. Regardless, they exist and are relevant.

Let me look inside the L-0903-2113.tgz (382KB, the 21:13 WU5 window bundle) — this is the most important for Q3 (post-restart reads / binding policy). Let me list contents and grep for debug/0x0010/switchConfig. I'll avoid loops. Let me use tar tzf and tar xzOf to grep.

Let me first see what's in each tgz.

=====BLOCK=====

The for-loop `$d` got mangled by cmd→zsh again, but the `.tgz` bundles exist (`L-0903-2113.tgz` = 382 KB covering the 21:13 WU5 window, created 08:57). Let me inspect their contents directly without a loop.



=====BLOCK=====

The tarball L-0903-2113.tgz contains log1.log (10MB, up to 23:00) and log.log (2.3MB, up to 23:29) — these are the SAME files as the live /config/zigbee2mqtt/log/2026-09-03.21-13-34/ (which I already parsed: log1.log 10000780B, log.log 2295745B). Exact same sizes! So this bundle is just a copy of the already-retained 21-13-34 window — no debug, nothing new. It's the same info-level content I already searched (genOnOffSwitchCfg 66, all failed except the 3 switchActions:2). So the archived bundle does NOT contain additional debug evidence — it's the same files.

The directories in /tmp (2026-09-03.21-04-51, L-0903-2113, etc.) are extracted copies of the same log windows. L-0903-2113 (dir mtime Sep3 23:00) is the 21-13-34 window. 2026-09-03.21-04-51 is the transition-install window. These are WU5's own snapshots, but of info-level logs.

Let me verify the L-0903-2113 dir contains the same content and check if there's a debug file hiding. Let me grep these bundle dirs for "debug" level and "0x0010"/switchConfig. Quick grep on the directory (real dirs now that I avoid the loop). Let me grep the extracted dirs for debug level and the key tokens.


=====BLOCK=====

The `L-0903-2113` bundle is a copy of the **same** `21-13-34` info-level files I already parsed (identical byte sizes 10000780 / 2295745). Let me confirm none of these bundles contain any debug-level frame with a 0x0010/switchConfig value.



=====BLOCK=====

Definitive confirmation for Q3 across BOTH the live rotation AND the archived WU5 host bundles:
- debug-level lines = 0 in every bundle (all info).
- switchConfig / standard_abi / readResponse / attributeReport / 0xff06 = 0 occurrences anywhere.
- The ONLY genOnOffSwitchCfg Read-result is `{"switchActions":2}`, 13× (same 13 I found).

So durable evidence of standard_abi (attribute 0x0010) = 2/2/2 being read back **DOES NOT EXIST** in any retained log or archived bundle. The 0x0010 reads were all attempted via `set 'read'` → `.read([16])` and all 9 timed out during the outage; and even the raw frame would need debug (never enabled). What exists is switchActions (0xff00) = 2 for the switch modes, 13× pre-outage.

I have now fully answered all six with evidence. Let me compile the final report. I'll be precise, cite sources + timestamps, distinguish device-confirmed vs cache-only, and note coverage.

Note on TZ: log line timestamps and dir names are LOCAL (+02:00 per host). The `docker inspect` StartedAt and `date -u` are UTC. lastSeen epoch 1788504969035 = 2026-09-04T06:56:09Z UTC = 08:56:09 local. I'll present local log timestamps as-is and give UTC conversions where useful.

Let me also note the important divergence: switch_custom.js live hash (e178e68a, 912912B) ≠ STATUS.md's recorded Gate-F hash (50d135be, 967427B). So switch_custom.js was REPLACED after Gate F (mtime Sep 1 19:07 — actually mtime Sep1 19:07 but STATUS.md Gate F recorded 50d135be... the file now is 912912/19:07 Sep1; STATUS.md said 967427 with mtime Aug22 for pre-F and 50d135be for F. The current on-disk switch_custom is 912912 B sha e178e68a mtime Sep 1 19:07 — differs from F's 50d135be. So switch_custom.js changed after Gate F too.). I'll report the observed live hash and flag the divergence from STATUS.md, without asserting which is correct — that's for the coordinator. But regardless, switch_custom.js is NOT what governs the device now (v5 overlay does, by priority+build match, proven by description string).

Let me write the final report now. I'll keep it structured, evidence-dense, with clear verdicts.

Let me assemble the numbers cleanly. I'll present it directly as my final response.

=====BLOCK=====

I have complete evidence across both the live Z2M rotation and the archived WU5 host bundles. Cleanup done (my temp copies removed; the pre-existing WU5 bundles left untouched). Final report:

---

# LivingRoomMainDimmer — Live Inspection Report (2026-09-04)

**Target:** `LivingRoomMainDimmer` / IEEE `0xa4c13843a9d40f85` / `iedhxgyi` `TS0726-3-BS` / nwk `17007`
**Method:** read-only. `docker exec` into `app_45df7312_zigbee2mqtt`, `docker cp` of log/state/db to the SSH add-on `/tmp` parsed with python3, direct `node`/`wget` probes. No device writes, no restarts, no config changes. All temp copies removed afterward.
**Timestamp note:** Z2M log line timestamps and `log/<dir>` names are **local (+02:00)**. `docker inspect` and `date -u` are **UTC**. I mark each. `lastSeen` epoch `1788504969035` = **2026-09-04T06:56:09Z** (= 08:56:09 local).

---

## 1. RUNTIME VERSIONS — ✅ matches supervisor expectation

| Component | Installed (authoritative, read from live container) | Source |
|---|---|---|
| Add-on | `45df7312_zigbee2mqtt` **2.14.0-1** | `ha apps list` / `ha apps info` |
| Image | `ghcr.io/zigbee2mqtt/zigbee2mqtt-aarch64:2.14.0-1` | `docker inspect` |
| Zigbee2MQTT | **2.14.0** | `/app/package.json` `"version": "2.14.0"` |
| zigbee-herdsman | **10.9.1** | `require('/app/node_modules/zigbee-herdsman/package.json').version` = `10.9.1` |
| zigbee-herdsman-converters | **26.103.0** | `require('.../zigbee-herdsman-converters/package.json').version` = `26.103.0` |

**Verdict:** Exactly what the supervisor expects (official **2.14.0-1 + ZHC 26.103.0**). `node_modules` reads (not just declared ranges) confirm the real installed versions. A newer `2.14.1-1` is *available* but **not** installed (`auto_update: false`).

**Version-change correlation to WU5** (from startup banners):
```
[2026-09-03 12:40:11] info: z2m: Starting Zigbee2MQTT version 2.13.0 ... zigbee-herdsman (10.8.0)
[2026-09-03 21:04:51] info: z2m: Starting Zigbee2MQTT version 2.14.0 ... zigbee-herdsman (10.9.1)
```
The **2.14.0-1 add-on update landed *during* WU5**, at the 21:04:51 "hardened transition install" restart (same timestamp as `bseed_ts0726_v5.js` mtime), not after. Everything from 21:04:51 onward is 2.14.0/10.9.1.

---

## 2. CONVERTER DEPLOYMENT STATE — live definition is the v5 overlay

`/config/zigbee2mqtt/converter_lib/` — **does not exist** (`ls: No such file or directory`).

`/config/zigbee2mqtt/external_converters/*.js`:

| File | Size | sha256 | mtime |
|---|---|---|---|
| `bseed_ts0726_v4.js` | 15678 | `d5b276dd967541e261e51606fdbf1e5412d55b7b660b8559d930e000eda04fa2` | Sep 2 10:42 |
| **`bseed_ts0726_v5.js`** | 37693 | **`a2a404974dcc3998a05b3862bfe2714aea197e0cf843eb03c191dee07a30fa92`** | **Sep 3 21:04 (WU5)** |
| `switch_custom.js` | **912912** | `e178e68a8b5cbb102686272a8dfee083088fee21356b22d0801840f0ef3fbbdb` | Sep 1 19:07 |
| `stb3l_125_zj_phase_fix.js` | 1675 | `b0aa0de4710d3db9426ccd001d931765d171ffc2dd58f0c1bd28682094df0e81` | (unrelated device) |
| `tuya_with_ota.js` | 3569 | `60ee3ddd55d89e4bec1e211e54573e26ae35d7e3dfda587b77e1e721958586a9` | (unrelated) |

**Which file governs the device — proven:**
- `bseed_ts0726_v5.js` header: *"BSEED TS0726-3-BS V5→V6 transition overlay."* Fingerprint requires `softwareBuildID ∈ {1.1.5-bseedv5, 1.1.6-bseedv6}` with **`priority: 100`**, and its `configure: async () => {}` is a deliberate **no-op**.
- Live resolution (`zigbee2mqtt/bridge/event` definition, `2026-09-04 08:56:11` local):
  ```
  definition.model = EC-GL86ZPCS31 | definition.vendor = Tuya-custom
  description = "BSEED 3-gang smart-light controller — protected mains control, direct binding and advanced configuration"
  ```
  That description string is **verbatim `bseed_ts0726_v5.js`** and is *different* from `switch_custom.js`'s `EC-GL86ZPCS31`, whose description is `"BSEED Echo Click / Scale 3-gang — Romasku custom firmware"` and whose fingerprint is generic `{manufacturerName:"iedhxgyi", modelID:"TS0726-3-BS"}` with no build/priority. Because the device is on `1.1.6-bseedv6`, the priority-100 v5 overlay wins over switch_custom's default-priority match.
- `bseed_ts0726_v4.js` matches only `1.1.4-bseedv4` → **dormant** (device is 1.1.6).

**Verdict:** The **currently live BSEED transition definition is `bseed_ts0726_v5.js`, sha256 `a2a40497…`**, resolving to model `EC-GL86ZPCS31` / vendor `Tuya-custom`. **Divergence from STATUS.md to flag:** STATUS.md recorded the Gate-F live `switch_custom.js` as `967427 B / sha256 50d135be…`; the on-disk file today is **`912912 B / sha256 e178e68a…` (mtime Sep 1 19:07)** — i.e. `switch_custom.js` has itself changed since Gate F. I did not reconcile which is correct, but it does not govern the target while v5 wins. (Consequence worth noting: with v5 governing, switch_custom's `configureReporting`/auto-rebind on EP3/EP4… is **not** running for this device — the hardened overlay suppresses it by design.)

---

## 3. STANDARD-ABI DURABLE EVIDENCE — ❌ NO device-confirmed `0x0010 = 2` exists

**Coverage first (log files considered, every `*.log`, Sep-3 + Sep-4 rotation, plus the archived host bundles):**

| File | lines | firstTS → lastTS (local) | level census |
|---|---|---|---|
| `2026-09-03.12-40-11/log2.log` | 11341 | 12:40:11 → 14:43:29 | debug=0 info=11311 |
| `.../log1.log` | 11078 | 14:43:30 → 16:48:56 | debug=0 info=11067 |
| `.../log.log` | 2567 | 16:48:56 → 17:15:09 | debug=0 info=2508 |
| `2026-09-03.17-15-11/log1.log` | 10580 | 17:15:11 → 19:09:30 | debug=0 info=10563 |
| `.../log.log` | 9623 | 19:09:30 → 20:42:36 | debug=0 info=9616 |
| `2026-09-03.20-42-43/log.log` | 1160 | 20:42:43 → 20:47:24 | debug=0 info=1154 |
| `2026-09-03.20-47-27/log.log` | 3425 | 20:47:27 → 21:04:49 | debug=0(incidental) info=3404 |
| **`2026-09-03.21-04-51/log.log`** (hardened install) | 1489 | 21:04:51 → 21:13:32 | debug=0 info=1475 |
| **`2026-09-03.21-13-34/log1.log`** (post-restart/binding-policy) | 11903 | 21:13:34 → 23:00:04 | debug=0 info=11644 |
| `2026-09-03.21-13-34/log.log` | 2752 | 23:00:04 → 23:29:09 | debug=0 info=2740 |
| `2026-09-04.06-07-16 / .06-09-32 / .06-12-42 / .06-16-01` | — | 06:07 → 08:57 | debug=0 |

Earliest retained Sep-3 log starts **12:40:11 local**; anything before that is rotated out. Archived host bundles `/tmp/L-0903-2113.tgz` (382 KB), `/tmp/2026-09-03.21-04-51/`, `/tmp/L-0904-0616.tgz` were inspected too — they are **byte-identical copies of the same info-level files** (log1.log=10000780 B, log.log=2295745 B). **Every retained log and every archived bundle is `log_level: info`; `advanced.log_level: info` is the current setting (`configuration.yaml` line 68).**

**Raw-ZCL-frame tokens searched across all logs and all bundles — all zero:**
```
readResponse=0  attributeReport=0  READRSP=0  REPORT=0  readRsp=0  commandResponse=0
switchConfig=0  standard_abi=0  0x0010(string)=0  0xff06(string)=0
```
Raw ZCL frames that carry attribute **bytes** require `debug`; debug was never enabled in the WU5 window, so no byte-level attribute value is logged at all.

**What the reads actually look like** (attribute IDs logged in decimal inside `genOnOffSwitchCfg.read([...])`; 65280=0xFF00, 65281=0xFF01, 65282=0xFF02, 65285=0xFF05, **16=0x0010**):

Every successfully-decoded `genOnOffSwitchCfg` result in *all* logs/bundles is **only** `{"switchActions":2}` — 13 occurrences, all pre-outage (attr 0xFF00, the `switch_*_mode` reads):
```
[2026-09-03 21:07:46] info: zhc:tz: Read result of 'genOnOffSwitchCfg': {"switchActions":2}   (…through)
[2026-09-03 21:15:10] info: zhc:tz: Read result of 'genOnOffSwitchCfg': {"switchActions":2}   (last success)
```

The `0x0010` read (`.read([16])`, issued via `Publish 'set' 'read'`) was attempted **9 times on EP1/EP2/EP3 — every one TIMED OUT, 0 successes**:
```
[2026-09-03 21:45:16] error: Publish 'set' 'read' to 'LivingRoomMainDimmer' failed: ...0xa4c13843a9d40f85/1 genOnOffSwitchCfg.read([16], {...}) failed (...target":24677... timed out after 10000ms)
[2026-09-03 21:45:26] error: .../2 genOnOffSwitchCfg.read([16]...) ... timed out after 10000ms
[2026-09-03 21:45:36] error: .../3 genOnOffSwitchCfg.read([16]...) ... timed out after 10000ms
   (retried EP1/2/3 at 21:50:47/51:07 and 22:01:58/02:08/02:28 — all timed out)
```
`.read([16]) total=9, inside failed/timeout=9, non-failed=0`.

**Verdict (Q3):** **Durable log evidence of `standard_abi = 2/2/2` on attribute 0x0010 DOES NOT EXIST** in any retained live log or archived WU5 bundle. The only "value 2" read back for EP1/2/3 is **`switchActions` (attribute 0xFF00) = 2** (13×, pre-outage). The `0x0010` reads all failed with timeout during the mesh outage, and no debug-level raw frames were ever captured to confirm the byte value. This is a genuine absence backed by the coverage table above, not rotation — but note the absence is partly manufactured by `log_level: info` (raw frames would need debug). To settle it authoritatively: enable `debug`, and after the route is healthy again, issue `zigbee2mqtt/LivingRoomMainDimmer/get` with the relevant properties (or the `set read` attr-16 path) and capture the `readResponse`.

---

## 4. ROUTE HEALTH NOW — ⚠️ device reachable & interviewing, but asymmetric; address churn + stale cache

- **Reachable now?** Yes, with caveats. The device **completed a successful interview** in the live window: `[2026-09-04 08:56:11] info: zh:controller: Succesfully interviewed '0xa4c13843a9d40f85'` / `z2m: Successfully interviewed 'LivingRoomMainDimmer'`. It also **responded** to a unicast read at 08:55:28 & 08:56:09 — the reply was `Status 'UNSUPPORTED_ATTRIBUTE'` for `genOnOff.read(["onOff"])` on **EP1** (EP1 is a *switch* endpoint; this is an application-layer response, **not** a timeout — proof the frame round-tripped).
- **`last_seen`:** `2026-09-04T06:56:09Z` (`database.db` `lastSeen=1788504969035`); `state.json` `linkquality: 100`.
- **Most recent transactions:** inbound `device_announce` `2026-09-04 08:55:26` and `08:56:06`; interview `08:56:11`; state republish `08:56:38`/`08:57:00` (cached, not fresh reads). **No successful `genOnOffSwitchCfg` read since 2026-09-03 21:15:10** — 0 in all Sep-4 logs.
- **ROUTE_ERROR last 12–24h:** our **old** nwk `24677` accumulated **848** `ROUTE_ERROR_MANY_TO_ONE_ROUTE_FAILURE` / `ROUTE_ERROR_SOURCE_ROUTE_FAILURE` on Sep 3, **first 15:39:38 → last 22:53:49 local** (the disclosed WU5 outage; fleet-wide total 881 incl. 49550/25408/18460). On Sep 4 there are **0** `ROUTE_ERROR` lines for the device, but early-morning commands (06:12–06:18 local) still `timed out` against the **stale** `target":24677` before it reappeared as `17007`.
- **Network-address churn:** DB now holds `nwkAddr = 17007`, while the outage-era commands and 24677 route errors were against its previous address. The device **rejoined with a new nwk**. Herdsman still cached `24677` for some sends → stale-route timeouts.
- **Parent / direct-child?** **Not observed.** No `parent`/route-table/`relay`/neighbor lines exist at info level (0 hits). Determining whether `0xa4c13843a9d40f85` is a direct child of coordinator `0xfdb1122d004b1200` or via a router requires a live ZDO `route_table`/`neighbor` read (debug or an active probe) — not authorized here.

**Verdict (Q4):** Reachable and interviewing successfully right now (08:56 local / 06:56Z), but link quality on outbound is **degraded/asymmetric** — inbound announces+responses arrive, outbound reads time out or hit the stale `24677`. Parent router is **undetermined** from retained data.

---

## 5. CURRENT PUBLISHED STATE — cache (state.json / database.db); note DB bind cache is empty

**Live definition** (device-confirmed via 08:56 bridge/event): `EC-GL86ZPCS31`, vendor `Tuya-custom` (v5 overlay — see §2).

**Firmware (cache, two surfaces agree, no fresh debug frame):**
- `database.db` (Z2M store): `swBuildId = '1.1.6-bseedv6'`, `dateCode = '20260903'`, `appVersion = 3`, `stackVersion = 2`, `zclVersion = 3`, `interviewCompleted = True`, `interviewState = 'SUCCESSFUL'`.
- `state.json` → `update`: `{"installed_version": 285356039, "latest_version": 285356039, "state": "idle"}`.
These corroborate the brief's expected **1.1.6-bseedv6 / build 285356039**, but both are Z2M cache (the `swBuildId`/`dateCode` from a prior genBasic interview; `dateCode` differs from STATUS.md's older `20260612`). Not byte-level re-confirmed this session.

**Binding-policy / per-endpoint policy (`state.json`, cache; `switchActions` had a device-read, the rest did not):**
```
switch_left_action_mode  = "Match local state"
switch_middle_action_mode= "Match local state"
switch_right_action_mode = "Toggle"                       → matches expected Match/Match/Toggle
abi_switchactions_left_switch_left   = {"switchActions": 2}
abi_switchactions_middle_switch_middle = {"switchActions": 2}
abi_switchactions_right_switch_right   = {"switchActions": 2}   ← device-read 0xFF00 on Sep 3
switch_{left,middle,right}_binded_mode = "Short press"           (0xFF05 reads all failed Sep 3 → cache)
switch_{left,middle,right}_relay_mode  = "Short press"
switch_{left,middle,right}_relay_index = "Left"/"Middle"/"Right" (relay_1/2/3)
```
The **"custom 0xff06 = 3/3/2"** is **not device-read anywhere** — `0xff06` appears 0 times in logs; only the cached `switchActions`/`binded_mode` fields above exist. device_config cache string:
```
device_config = "iedhxgyi;TS0726-3-BS;LC4;SB1u;RC2;IC0;SB7u;RC3;ID7;SB4u;RD2;IB5;M;"   (cache; differs from STATUS.md's deployed string)
```

**Per-endpoint mains/indicator + logical state (`state.json` cache — relay/indicator reads never succeeded this window):**
```
relay_left_physical_mode   = "Always on"                 relay_middle_physical_mode = "Always on"
relay_right_physical_mode  = "Follow logical state"      (divergent key relay_right_physical_mode_relay_right = "Always off")
relay_left_indicator_mode  = "Binding status"  relay_left_indicator = "OFF"  relay_left_binding_intent = "OFF"
relay_middle_indicator_mode= "Binding status"  relay_middle_indicator = "OFF" relay_middle_binding_intent = "OFF"
relay_right_indicator_mode = "Physical output" relay_right_indicator = "OFF"  relay_right_binding_intent = "ON"
raw_ep4/5/6_onoff_physical_relay_{left,middle,right} = {"65283":1, "onOff":0}
state_relay_left = "OFF"  state_relay_middle = "OFF"  state_relay_right = "OFF"
power_on_behavior_relay_{left,middle,right} = "previous"
```

**Bind/report cache is currently EMPTY (contradicts STATUS.md, cache-only):**
`database.db` for the device now shows **`binds: []`, `configuredReportings: []`, and empty cluster attributes on EP1–EP6** (EP1 only `{genOnOff:{attributes:{}}}`). The rejoin (nwk 24677→17007) reset Z2M's endpoint cache after this boot. This does **not** restate the on-device ZDO binding table (28/32 last authoritative 2026-08-31); it is cache and is now stale/empty. The `bridge/devices` binding projection could not be read without an MQTT subscribe (broker creds withheld).

**Verdict (Q5):** Binding-policy projection **Match/Match/Toggle** confirmed *in cache*. `switchActions=2` is the only value with retained device-read support. All indicator/relay/`0xff06`/`binded_mode`/`device_config` figures are **cache-only**; DB bind cache is presently empty and must not be read as the device's actual binding table.

---

## 6. SERVICES — ✅ healthy

- **Z2M frontend:** `wget -S http://127.0.0.1:8099/` → **`HTTP/1.1 200`**, `content-type: text/html`, `content-length: 886`, `Date: Fri, 04 Sep 2026 07:06:04 GMT`. Port **8099** = `ingress_port` (`ha apps info`), container listener `:::8099 node`.
- **Container:** `app_45df7312_zigbee2mqtt`, `Up … (healthy)`, `docker inspect`: **`Health=healthy`, `Restarts=0`, `StartedAt=2026-09-04T04:15:59.291563239Z`**.
- **Uptime / restart correlation:** container last (re)started **04:15:59Z today** = the `2026-09-04.06-16-01` (local) log window. This follows a **host boot ≈04:05Z** (`uptime` showed 2:48 at 06:53Z), which produced rapid prior starts `06:07`/`06:09`/`06:12` local then settled at `06:16`. So the *current* instance is not from an add-on update — the 2.14.0-1 update happened earlier, at the **21:04:51 Sep-3 WU5 restart** (§1). `auto_update: false`.

---

## What I could not verify / what would settle it
1. **`0x0010`/standard_abi byte value** — never debug-logged and every read timed out. Settle: enable `debug`, restore a healthy route, then `get` (or `set read [16]`) on EP1/2/3 and capture `readResponse`.
2. **Parent router / direct-child** — no ZDO data at info level. Settle: live `route_table`/`neighbor` read (needs debug or active probe).
3. **Actual on-device binding table** — DB bind cache is empty post-rejoin; `bridge/devices` projection needs an MQTT retained-topic read (broker creds withheld). Settle: paged ZDO `binding_table` read.
4. **Firmware `1.1.6-bseedv6` / build `285356039`** — cache-corroborated (DB + state.json agree) but not re-fetched via a raw `genBasic` frame this session.
5. **`switch_custom.js` hash drift** vs STATUS.md (now `e178e68a…`/912912 B vs Gate-F `50d135be…`/967427 B) — reported as observed; not adjudicated (it does not govern the target while v5 wins).