## EXECUTOR RETURN — audit `5549663848` items 1–6 all addressed; V7 acceptance functionally COMPLETE; one operator-vs-checklist state item flagged; HA v2 staged onto current main, awaiting deploy go

### Items 1–6 status
1. **Evidence banked & pushed.** Executor branch `executor/ts0726-v6-software-live-2026-09-03`, chain: `9c01af7` (raw press-window logs, transaction log `restore-result.json` 16/16 ok, pre-restore empty-table dump, **live post-restart readback 20/20 bindings = baseline set**), `981ce47`, `91c01e7`, `c6b80a7`. Container `/tmp` was destroyed by an **unplanned host restart 06:07 UTC Sep-5**; reconstructed probe JSONs are marked verbatim-with-provenance in `accept/raw-logs/PROVENANCE.md`; the raw coordinator-log dumps are originals.
2. **Topology accounting corrected — and your skepticism was warranted.** `attr0` is **NOT** a duplicate of `onOff`. Proof chain (raw `database.db.backup` pre-rejoin records + herdsman 10.9.1 source): each relay EP carried two reportings — attrId `0x0000` (`onOff`) **and vendor attrId `0xFF02`** (indicator LED-mode); `0xFF02` is absent from herdsman's genOnOff table so the bridge getter mints the fallback name `attr${arrayIndex}` (`endpoint.js:89`). My "2 attr0 / 20+10 / redundant duplicate" wording was wrong: baseline = **20 bindings + 9 reportings**, three `attr0` = three `0xFF02`. The restore is therefore **bindings 20/20 exact** (re-verified post-restart incl. LinearDimmer/group-25/self entries) but **reportings 6/9 functional** — the 3 vendor LED-mode reports were not re-added, cannot be re-added via the supported bridge API (herdsman rejects unknown genOnOff attrs), are not on any control/state/action path (all live-verified working), and I will not re-introduce them without your explicit go. "Exact/byte-equivalent restore" wording retracted. Full table: `accept/TOPOLOGY-ACCOUNTING-CORRECTION.md`.
3. **LEFT post-restore tail executed and captured** via bounded MQTT observer (no log-level change, no mutation): LEFT short => `switch_0_press/on/release` + **LinearDimmer target state exactly once** (`brightness=68`); LEFT hold => `switch_0_brightness_move_up` + target `68→114→203` + `switch_0_brightness_stop` on release; RIGHT presses => **multistate press/release ONLY**, no `switch_2_*` on/off, no Move/Stop — master gate reconfirmed *with* coordinator binds live (a strictly stronger test than pre-restore). Raw: `accept/raw-logs/left-events.jsonl`, analysis: `ACCEPTANCE-LEFT-TAIL.md`. Final battery: `0xff00=1/1/1`, `0xff05=3/3/0`, `0xff06=3/3/2`, EP4=0 EP5=0.
   - **Resting-state note (operator decision, attributed not glossed):** final read 07:04:02Z shows **EP6 relay_right = ON**. The operator pressed RIGHT a second time (deliberately, to confirm the relay responds to the button), declared this **"a pass,"** and instructed me to **stop checking the buttons — they work**. I have not forced it and will not without instruction. This is the one place the operator's accepted end-state differs from your checklist's literal `RIGHT OFF/OFF/OFF`: I flag it as an **operator-accepted deviation**, not an unresolved executor item, and hand the OFF-vs-ON closure gate to you (see request (a)). The functional proof that matters for V7 — RIGHT actuates its **local** relay exactly once per press while emitting **zero** bound OnOff/Level — is satisfied and banked regardless of the resting polarity.
4. **0xff03 no longer probed** — battery surfaces are `0xff00/0xff05/0xff06/onOff` only (`accept/battery2.js`); policy proven via the supported store/select path. All 0xff03-class errors classified `EXPECTED/PROBE-NOISE`.
5. **HA v2 staged onto current main** — worktree, branch `integration/ts0726-ha-v2-on-main-2026-09-05`, single commit **`76bbecd`** directly on `bf41dc3` (= origin/main at execution; parent equality proven; zero file overlap with main's 3 rodret commits so main's work fully preserved). Ported 4 files **byte-identical to `9472e5b`** (empty diff). Contract suite **40/40** (incl. the 5 v5 assertions *and* main's rodret tests). Real `check_config`: clean tree **exit 0**, negative control (corrupted copy) **exit 1 `Failed config`** — harness verified genuine. Scope greps on the result: no `state_relay_right`/`relay_right_binding_intent` anywhere; `physical_mode` appears only as fail-closed `states()==` reads; no bind/unbind/group/device_config ops (sole textual hit is the description promising not to); reconcile payloads `state_relay_left/middle` only. **Local only — nothing pushed, live HA untouched.** Full proof: `accept/HA-V2-INTEGRATION-PROOF.md`.
6. **Attribute classification delivered** — 122 log lines / 16 patterns scanned read-only across 10 windows (`accept/raw-logs/unsupported-attr-scan.txt`, rubric table in `accept/LATENCY-ERROR-CAPTURE.md`). Headline for the operator's log sighting: the **recurring** error-level `Failed to poll currentLevel` on EP4/EP5 is **Z2M core `extension/bind.js`** initial-state polling fired by the *restored, documented* `genLevelCtrl→coordinator` binds — the firmware *advertises* genOnOff-cluster `genLevelCtrl` on relay EPs but returns `UNSUPPORTED_ATTRIBUTE` for `currentLevel` (descriptor/attr mismatch, V6-era config had the same binds; BSEED converter files contain **zero** `currentLevel` refs; same EPs answer `onOff` cleanly seconds before/after). Classification: `EXPECTED/BIND-ARTIFACT` — not converter-surface-bug, not V7 regression. Remaining occurrences: probe noise (retired surfaces) + transient reconnect artifacts from the 06:07 host restart. **No converter-surface-bug or firmware-regression class hits at all.**
   Latency (diagnostic, non-gate): device→coordinator leg **50–200 ms**; coordinator→target visible report **~1.1–1.4 s**; plus known `ROUTE_ERROR_MANY_TO_ONE_ROUTE_FAILURE` history → per your decision rule: mesh/target-side leading suspect; carried into existing mesh investigation. Not prolonging here.

### Safety account for this work unit
No reflash. No topology change since the operator-authorized restore (readbacks only). No HA write of any kind (staging is local-repo). `log_level=info` (proven). Only device reads + the operator-initiated button presses this window.

### Requested from Supervisor (single ruling unblocks everything)
(a) Resolve the RIGHT-resting-OFF gate vs operator "pass" (one press / one authorized SET / waive to ON); (b) go for HA v2 deploy from `76bbecd` (I'll run live contract verification + final readback battery immediately after; per audit #5, workflow trigger widening at merge is yours). Everything else for closure is banked and green.
